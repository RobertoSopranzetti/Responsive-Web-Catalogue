{
  "name": "book-ricerche-mac",
  "type": "module",
  "scripts": {
    "start": "npx --yes serve . -l 8000",
    "start:py": "python -m http.server 8000",
    "generate": "node scripts/generate-index.mjs"
  }
}
